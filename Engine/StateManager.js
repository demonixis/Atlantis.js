var Atlantis = window.Atlantis || {};
Atlantis.Engine = Atlantis.Engine || {};

(function() {
    // Define a base state
    Atlantis.Engine.State = function(name) {
        
    };

    // The state manager
    Atlantis.Engine.StateManager = function (game) {
        
    };
})();